# 前端环境变量示例（复制为 .env / .env.development / .env.production 使用）
# 开发服务器端口（仅 vite dev server 使用）
DEV_PORT=5174
# 开发时代理转发到的后端服务地址（仅 vite dev proxy 使用）
API_TARGET=http://localhost:3000
# 前端请求后端的基地址（注入前端 bundle，需 VITE_ 前缀）
# 开发/生产默认相对路径 /server/api（由 Vite 代理或 nginx 反代转发到后端）
VITE_API_BASE=/server/api
